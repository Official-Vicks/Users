# Users API (FastAPI Project)  
A simple user management API built with FastAPI, SQLAlchemy and SQLite — demonstrating full-stack CRUD operations, designed as a learning project by [Your Name].

## 🔧 Tech Stack  
- **Backend**: Python 3.x, FastAPI  
- **Database / ORM**: SQLite + SQLAlchemy  
- **Frontend**: HTML + Bootstrap + JavaScript Fetch API (if you built UI)  
- **Other tools**: CORS middleware, Pydantic schemas, GitHub for version control  

## 🚀 Features  
- Create, Read, Update, Delete (CRUD) users  
- Data persistence in SQLite database  
- Request validation and response models via Pydantic  
- Basic API error handling and HTTP status codes  
- CORS enabled to allow front-end / browser requests  
- (Optional) Simple front-end UI interacting with the API  

## 🧪 Getting Started  
### Prerequisites  
- Python 3.x installed  
- (Optional) Virtual environment setup  
- `git` for version control  

### Installation  
```bash
git clone <repo-url>
cd Users
python -m venv venv              # Create virtual env
source venv/bin/activate         # On Windows: venv\Scripts\activate
pip install -r requirements.txt
